module.exports = function (input) {
    const n = parseInt(input[0]);
    const arr = input[1].split(" ").map(Number);

    // -------------------------------------------





//--------------------------------------
    return [n,arr.join(" ")];
};